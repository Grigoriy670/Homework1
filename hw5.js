const arr = [1, 5, 4, 10, 0, 3];
for(let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
    if (arr[1] === 10) {
        break;
    }
}

const arr = [1, 5, 4, 10, 0, 3];
console.log( arr.indexOf(4) );
